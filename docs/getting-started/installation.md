# Installation

## Prerequisites

- Python 3.12+
- uv (recommended) or pip
- AWS credentials configured

## Install with uv

```bash
uv sync
```

## Install with pip

```bash
pip install -e ".[dev]"
```

## Pre-commit hooks

```bash
pre-commit install
```
